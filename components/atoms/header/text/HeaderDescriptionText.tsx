// component
const HeaderTextDescription = ({ description }: { description: string }) => {
  return (
    <p className="text-sm font-light text-center mt-4 mb-5">{description}</p>
  );
};

export default HeaderTextDescription;
